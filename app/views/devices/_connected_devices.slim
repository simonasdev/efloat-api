h2 Connected devices (#{devices.size})
pre
  = render_connected_devices(devices)

hr

h2 Not disabled disconnected devices (#{disconnected_devices.size})
pre
  = render_disconnected_devices(disconnected_devices)
